# Credentials: Wi-Fi password and the cell's frame-authentication key.
# The versioned template is des_distributed/secrets.example.h.
secrets.h

# Local Claude Code settings (per-machine permissions).
.claude/settings.local.json
